const useColorUtils = () => {

    const hlsToHexColor = () => {
        return '';
    };

    const getColorList = () => {
        return [];
    };

    return {
        getColorList,
        hlsToHexColor,
    };
};

export default useColorUtils;
